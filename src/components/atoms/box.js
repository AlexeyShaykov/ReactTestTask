import styled from 'styled-components';
import { space, width, textAlign, order } from 'styled-system';

export const Box = styled.div`
  ${space};
  ${textAlign};
  ${width};
  ${order};
  position: relative;
`;
